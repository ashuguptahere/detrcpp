build/
build-*/
out/
cmake-build-*/
.vscode/
.idea/
*.swp
*.o
*.obj
*.so
*.dylib
*.dll
*.a
*.lib
*.exe
*.pdb
*.ilk

# vcpkg
vcpkg_installed/

# Python (scripts/)
__pycache__/
*.pyc
.venv/
.env

# Runtime artifacts
runs/
weights/*.pt
weights/*.onnx
weights/*.engine
# Downloaded model checkpoints (scripts/download_models.cmake -> models/). Keep the
# directory + README, ignore the weights themselves.
models/*.pth
models/*.pt
models/*.onnx
models/*.engine
models/*.safetensors
*.tracy
*.nsys-rep
*.qdrep

# Datasets are external (anchored to repo root so src/data, include/detr/data
# — which are source, not datasets — are NOT ignored).
/data/
/datasets/

# Local secrets — never commit (gitleaks is the authoritative gate)
.env*
*.pem
*.key
*.p12
*.pfx
*.p8
*.jks
*.keystore
*.crt
id_rsa
id_ed25519
id_*
.netrc
.npmrc
.pypirc
*.tfstate
*credentials*.json
*service-account*.json
secrets/

# Misc
.DS_Store
Thumbs.db
